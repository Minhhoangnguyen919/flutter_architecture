enum Flavor {
  develop,
  production,
}

enum BottomTabItem {
  homeView,
  bagView,
  searchView,
  userView,
}

enum BuildMode {
  debug,
  profile,
  release,
}

enum OnOffState { on, off }

enum Level {
  error,
  warn,
  info,
  debug,
  trace,
}
