enum Flavor {
  develop,
  production,
}

enum BottomTabItem {
  homeView,
  bagView,
  searchView,
  userView,
}

enum BuildMode {
  debug,
  profile,
  release,
}

enum Level {
  error,
  warn,
  info,
  debug,
  trace,
}

enum QRWidgetViewType {
  qrView,
  scanView,
}

enum QRScanResult {
  idle,
  scanning,
  scanSuccess,
  scanFailure,
}